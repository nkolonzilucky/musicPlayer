import type { NextPage } from 'next';
import Image from "next/image";



const PlayButton:NextPage = () => {
  	return (
    		<Image className="" width={200} height={200} alt="" src="playButton.svg" />);
};

export default PlayButton;
