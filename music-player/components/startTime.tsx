import type { NextPage } from 'next';



const StartTime:NextPage = () => {
  	return (
    		<div className="relative text-[1.146rem] font-semibold font-inter text-white text-left">2:20</div>);
};

export default StartTime;
