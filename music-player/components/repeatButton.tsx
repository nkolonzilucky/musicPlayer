import type { NextPage } from 'next';
import Image from "next/image";



const RepeatButtonIcon:NextPage = () => {
  	return (
    		<Image className="" width={50} height={50} alt="" src="repeatButton.svg" />);
};

export default RepeatButtonIcon;
